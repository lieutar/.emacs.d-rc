(defvar rc-sync-file (epand-file-name  "~/.emacs.d/rc/sync.sexp"))
(defvar rc-sync-files-list
  "http://www.kael/~lieutar/emacs/rc/files.php?mode=list"
  )
(defvar rc-sync-options nil)


(defun rc-sync-save-options ()
  ())
(defun rc-sync-load-options ()
  ())


(defun rc-sync ()
  (interactive))
(defun rc-sync-reset-options ()
  (interactive))


